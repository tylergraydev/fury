---
title: Environment Variables
description: Configure environment variables for scripts and agents
---

## Custom environment variables

Set per-repository environment variables in **Settings > Environment Variables**. These are passed to all scripts and agent processes for that repository.

## Automatic variables

Fury automatically provides these environment variables to scripts:

| Variable | Value |
|----------|-------|
| `FURY_WORKSPACE_NAME` | Name of the current workspace |
| `FURY_WORKSPACE_PATH` | Absolute path to the workspace worktree |
| `FURY_ROOT_PATH` | Absolute path to the main repository |
| `FURY_DEFAULT_BRANCH` | Repository's default branch (e.g., `main`) |
| `FURY_PORT` | Base port allocated to the workspace (e.g., `10000`) |

## Provider variables

When using a non-default [provider](/guides/providers), the provider's environment variables (API keys, endpoints) are passed to the agent process.

## Using in scripts

Reference these variables in your scripts like any shell variable:

```bash
echo "Working in $FURY_WORKSPACE_PATH"
echo "Default branch is $FURY_DEFAULT_BRANCH"
```
